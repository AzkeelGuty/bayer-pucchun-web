# Base de datos
1. Cree una BD MySQL/MariaDB con `utf8mb4`.
2. Importe `schemas/001_schema.sql`.
3. Importe `seeders/001_seed.sql`.
4. Cambie inmediatamente la contraseña del usuario administrador.

Usuario inicial: `admin@local.test` / `Admin123*` (solo bootstrap; cambiar en producción).
